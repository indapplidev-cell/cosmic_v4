# -*- coding: utf-8 -*-
"""
Runtime profile builder for level 2.

EN: Defines the local target-score level profile used by the default provider.
RU: ?????????? ????????? ??????? ?????? ? target-score ????? ??? provider ?? ?????????.
"""

from __future__ import annotations

from engine.levels.contracts import LevelRuntimeProfile


def build_level_2_profile() -> LevelRuntimeProfile:
    """
    Build the runtime profile for the second local level.

    EN: Returns level_2 with half base vertical speed and a target score goal.
    RU: ?????????? level_2 ? ?????????? ??????? ???????????? ????????? ? ????? ?? ?????.
    """
    return LevelRuntimeProfile(
        level_id="level_2",
        initial_speed_y_factor=0.5,
        goal_type="target_score",
        time_limit_sec=None,
        target_score=100,
    )
